请仔细验证以下测试用例的正确性。

题目分析：
{problem_analysis}

生成的测试用例：
{test_cases}

**验证要求**：
请对每个测试用例进行以下验证：

1. **输入验证**：
   - 输入格式是否符合题目要求？
   - 输入是否在题目约束范围内？

2. **期望值计算验证**：
   - 请使用暴力方法或数学计算重新计算每个用例的期望值
   - 确保期望值完全正确，没有计算错误
   - 检查输出格式是否与题目要求一致（如数组格式、空格、换行等）
   - 对于复杂计算，建议分步验证

3. **边界检查**：
   - 边界值是否正确处理了极端情况？
   - 是否存在整数溢出的可能？
   - 空值/零值处理是否正确？

4. **覆盖度检查**：
   - 测试用例是否覆盖了不同场景？
   - 是否存在重复或冗余的用例？

**请特别注意**：
- 数值计算要精确，不要估算
- 输出格式要严格匹配题目要求
- 如果有任何不确定，请重新计算验证

请以 JSON 格式返回验证结果：
```json
{{
    "validation_passed": true/false,
    "validated_test_cases": [
        {{
            "input": "测试输入（保持原样或修正后）",
            "expected": "期望值（保持原样或修正后）",
            "description": "测试描述",
            "category": "general/edge",
            "validation_notes": "验证说明，如有修正请说明原因"
        }},
        ...
    ],
    "corrections_made": [
        {{
            "index": 0,
            "original_expected": "原期望值",
            "corrected_expected": "修正后期望值",
            "reason": "修正原因"
        }}
    ],
    "validation_summary": "验证总结：所有用例正确/修正了X个用例"
}}
```
