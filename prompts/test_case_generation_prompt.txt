基于以下题目分析，生成5个测试用例。

要求：
1. **一般情况1-2个**：符合题目描述的常规输入，覆盖常见场景
2. **边界情况2-3个**：
   - 最小值/最大值（如数组长度为1、数组长度为最大限制）
   - 空输入/零值
   - 特殊数值（如负数、0、重复元素）
   - 极端情况（如极大数、极小数）

**重要提示**：
- 测试用例必须具有代表性，能够充分验证代码正确性
- 计算期望值时请仔细验证，确保输出格式完全正确
- 注意数值计算的精度问题（如浮点数保留位数）
- 数组/列表输出注意格式：[1, 2, 3] 还是 1 2 3
- 字符串输出注意是否包含换行或空格
- 建议先用简单的暴力方法手动验证期望值的正确性
- **每个测试用例必须说明属于哪类情况**

题目分析：
{problem_analysis}

请用以下 JSON 格式返回测试用例：
```json
{{
    "test_cases": [
        {{
            "input": "测试输入1",
            "expected": "期望输出1",
            "description": "测试描述（说明属于一般情况还是边界情况，以及为什么选这个用例）",
            "category": "general/edge"
        }},
        ...
    ]
}}
```

**验证要求**：
在生成测试用例后，请再次检查每个用例：
1. 输入格式是否符合题目要求？
2. 期望值是否经过正确计算？
3. 输出格式是否完全正确？
4. 是否覆盖了不同的场景？
